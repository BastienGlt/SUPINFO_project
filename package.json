{
  "name": "supinfo-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix backend",
    "install:all": "npm install --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
