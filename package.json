{
  "name": "supinfo-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "web": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "mobile": "concurrently \"npm run dev --prefix backend\" \"npm run lan --prefix frontend-mobile\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix frontend-mobile"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
